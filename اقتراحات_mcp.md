# اقتراحات تطوير الـ MCP

## المهمة الحالية
محاولة جمع سياق المشروع باستخدام أدوات `flutter-explorer-mcp` (مثل `flutter_get_stats` و `flutter_search`) عند بدء العمل بناءً على التعليمات في `AGENTS.md`.

## المشكلة/القصور
- أداة `flutter_get_stats` ترجع "Index not found" في بعض الأحيان.
- أداة `flutter_search` ترجع "Index not found. Please build the index in VS Code first."
- أداة `flutter_get_pubspec` ترجع "pubspec.yaml not found" على الرغم من وجود الملف في الجذر.

## الاقتراحات التقنية
1. **بناء الفهرس تلقائياً**: يجب أن يحاول الخادم بناء الفهرس إذا لم يكن موجوداً بدلاً من طلب بنائه يدوياً.
2. **تحسين البحث**: إضافة دعم للبحث عن طريق الملفات (File-based fallback) في حال فشل الفهرس.
3. **دعم الـ AI**: توفير أدوات تسمح للـ AI بتحليل الأخطاء المسجلة في `errror.md` واقتراح حلول.
4. **سجل الفشل**: توثيق أي فشل في أدوات MCP في هذا الملف تلقائياً من قبل العملاء (Agents).
