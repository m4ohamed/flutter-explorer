# 🚨 سجل الأخطاء والوقاية منها (error.md)

تم إنشاء هذا الملف بناءً على تعليمات المستخدم الصارمة لتسجيل الأخطاء المحتملة في `dartParser.ts` وكيفية الوقاية منها وإصلاحها قبل الشروع في كتابة الكود، تفادياً للوقوع فيها.

---

## 1. خطأ التراجع الكارثي في التعبيرات النمطية (ReDoS / Catastrophic Backtracking)
- **الخطأ المحتمل:** استخدام أنماط مثل `[\s\S]*?` داخل الأقواس في `P.method` و `P.ctor`. عند وجود ملفات Dart ضخمة أو أسطر معقدة لا تطابق النهاية المتوقعة، يقوم محرك الـ Regex بعمليات تراجع أسية تؤدي إلى تجميد الـ Extension (100% CPU).
- **الوقاية والإصلاح:** استبدال `[\s\S]*?` بأنماط محددة ومحكومة مثل `\((?:[^)(]+|\([^)(]*\))*\)` لدعم المعاملات التي تحتوي على أقواس متداخلة (مثل `void Function(int)`) دون أي تراجع كارثي.

---

## 2. فقدان أو تضارب مرجعيات النطاق (Scope Name Collisions)
- **الخطأ المحتمل:** الاعتماد على البحث بالاسم `result.classes.find(c => c.name === popped.name)` داخل دالة `syncBraces`. في حال وجود `extensionType` أو كلاسات/دوال بنفس الاسم في نطاقات مختلفة، يتم تعيين `lineEnd` للعنصر الخاطئ.
- **الوقاية والإصلاح:** تضمين مرجع مباشر للعنصر `ref` داخل كائن `ScopeFrame`. عند إغلاق النطاق (pop)، يتم التعديل مباشرة عبر المرجع O(1) `popped.ref.lineEnd = lineNum_` مما يضمن الدقة المتناهية والسرعة.

---

## 3. تلوث عداد الأقواس في دالة `extractEnumValues`
- **الخطأ المحتمل:** المرور على الأسطر الأصلية `lines[i]` التي قد تحتوي على تعليقات `// {` أو نصوص `'}'`. هذا يخدع العداد `depth` ويؤدي إلى قطع التحليل قبل اكتمال الـ Enum أو الفشل في استخراج القيم.
- **الوقاية والإصلاح:** تمرير الأسطر المطموسة `maskedLines` واستخدامها لتتبع الأقواس `depth`، بينما يتم استخدام الأسطر الأصلية فقط عند استخراج اسم القيمة الفعلي.

---

## 4. طمس سياق الاستدعاءات في `extractFunctionCalls`
- **الخطأ المحتمل:** استخدام `maskedLines` كمصدر لقص سياق الاستدعاء `context`. يظهر للمستخدم في الـ IDE سياق مليء بالمسافات الفارغة بدلاً من النصوص والتعليقات الحقيقية.
- **الوقاية والإصلاح:** فصل مصفوفة التطابق `maskedLines` عن مصفوفة العرض `lines` وتمريرهما معاً للدالة، بحيث يتم البحث في المطموس والقص من الأصلي.

---

## 5. التعقيد الزمني O(Lines × Symbols) في `analyzeUsages`
- **الخطأ المحتمل:** عمل حلقة تكرارية لكل سطر، وبداخلها حلقة تكرارية لكل رمز `sym.pattern.test(mLine)`. في المشاريع الكبيرة، يؤدي ذلك إلى مئات الآلاف من اختبارات الـ Regex البطيئة.
- **الوقاية والإصلاح:** بناء `Map` للرموز، واستخراج الكلمات الفريدة من السطر مرة واحدة عبر `mLine.match(/\b[A-Za-z_]\w*\b/g)`، ثم البحث المباشر في الـ Map بتعقيد O(1).

---

## 6. الفشل الصامت أو الانهيار التام بسبب غياب Error Handling
- **الخطأ المحتمل:** حدوث استثناء (Exception) غير متوقع أثناء تحليل ملف Dart ذي بنية شاذة، مما يؤدي إلى تعطل عملية الفهرسة (Indexing) بالكامل في الـ Extension.
- **الوقاية والإصلاح:** تغليف المحرك الرئيسي لـ `parse` بـ `try...catch` وإرجاع كائن فارغ آمن يحتوي على القوائم الأساسية لضمان استمرار عمل الـ Extension والـ MCP Server بدون توقف.

---

## 7. عدم اكتمال طمس التعليقات المتداخلة والـ Raw Strings
- **الخطأ المحتمل:** تجاهل نصوص `r'...'` والتعليقات المتداخلة `/* ... /* ... */ ... */` في `preprocessSource` مما يسبب False Positives.
- **الوقاية والإصلاح:** تحديث دالة التنظيف لتشمل كافة صيغ الـ Raw Strings وتدعم التعليقات المتداخلة برمجياً.
